[project]
name = "check"
version = "0.1.0"
description = "Enter your id and return the most recently generated number"
requires-python = ">=3.11"
dependencies = [
    "nats-py>=2.14.0",
    "redis>=8.0.0",
]
